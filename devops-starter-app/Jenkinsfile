pipeline {
  agent any

  stages {
    stage('Clone') {
      steps {
        git 'https://github.com/awslab-devops/devops-starter-app.git'
      }
    }

    stage('Build Docker Image') {
      steps {
        sh 'docker build -t devops-app .'
      }
    }

    stage('Tag Image') {
      steps {
        sh 'docker tag devops-app docker21devops/devops-app'
      }
    }

    stage('Push to Docker Hub') {
      steps {
        sh 'docker push docker21devops/devops-app'
      }
    }
  }
}
